#include "main.h"
/**
 * add - is a function to add two interges.
 * @number1: onr of the two variables
 * @number2: the other vaiable.
 * Return: number1 and number2.
 */
int add(int number1, int number2)
{
	return (number1 + number2);
}
