#include "main.h"
/**
 * _abs - is a function to print out the absolute value of n.
 * @my: is the number
 * Return: n for absolute value, and return x for -ve value.
 */
int _abs(int my)
{
	if (my < 0)
{
	int abs_val1;

	abs_val1 = my * -1;
	return (abs_val1);
}
{
return (my);
}
}
