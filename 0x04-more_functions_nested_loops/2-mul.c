#include "main.h"

/**
 * mul - is a function to muliple two numbers.
 * @a: is a variable that represents the first number.
 * @b: is avariable that represents the second numbers.
 * Return: c which is the result of the mulipling.
 */

int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
